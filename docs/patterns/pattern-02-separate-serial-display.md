# Pattern 2: Separate Serial Display

Pattern 2 retains the source design with separate hour and minute display axes on a serial transmission route. Its public adapter runs the frozen base stage, complete bridge stage, and bridge checklist in one temporary output directory, then atomically publishes every declared source artifact.
