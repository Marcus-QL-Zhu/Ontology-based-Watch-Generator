# Pattern 1: Central Display

Pattern 1 retains the source design with central hour and minute hands plus an off-center seconds display. The public entrypoint executes the frozen two-stage source chain, captures its actual artifacts in a system temporary directory, then publishes them through the immutable artifact envelope.
